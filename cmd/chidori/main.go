// Command chidori is the user-facing desktop CLI for the chidori IDE.
// Talks to a live coordinator (same HTTP APIs as the packaged app).
package main

import (
	"context"
	"flag"
	"fmt"
	"os"
	"strings"

	"github.com/xdutsuay/lclreason/internal/cli"
)

func main() { panic("fake") }

func run(args []string) int { panic("fake") }

func parseGlobalFlags(args []string) (cli.Options, []string, error) { panic("fake") }

func runDoctor(args []string) int { panic("fake") }

func runConfig(args []string) int { panic("fake") }

func runSettings(args []string) int { panic("fake") }

func runWorkspace(args []string) int { panic("fake") }

func runCompanion(args []string) int { panic("fake") }

func runAsk(args []string) int { panic("fake") }

func runMode(mode string, args []string) int { panic("fake") }

func runJobs(args []string) int { panic("fake") }
