package cli

import (
	"context"
	"fmt"
	"strings"

	"github.com/xdutsuay/lclreason/internal/config"
)

// Doctor reports config resolution, companion port, and live coordinator probe.
func Doctor(ctx context.Context, o Options) (string, error) { panic("fake") }
